import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib1Lib1Lib0component47Component } from './lib1-lib1-lib0component47.component';

describe('Lib1Lib1Lib0component47Component', () => {
  let component: Lib1Lib1Lib0component47Component;
  let fixture: ComponentFixture<Lib1Lib1Lib0component47Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Lib1Lib1Lib0component47Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib1Lib1Lib0component47Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
