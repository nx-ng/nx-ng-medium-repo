import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Lib1Lib1Lib1component57Component } from './lib1-lib1-lib1component57.component';

describe('Lib1Lib1Lib1component57Component', () => {
  let component: Lib1Lib1Lib1component57Component;
  let fixture: ComponentFixture<Lib1Lib1Lib1component57Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ Lib1Lib1Lib1component57Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(Lib1Lib1Lib1component57Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
